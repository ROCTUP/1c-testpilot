[project]
name = "1c-testpilot"
version = "1.7.0"
description = "MCP-сервер для управления тест-клиентом 1С:Предприятие"
readme = "README.md"
license = "AGPL-3.0-only"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [{ name = "ROCTUP" }]
keywords = ["1c", "mcp", "openai", "gateway", "bsl", "testclient", "testing", "ui-automation", "claude"]
dependencies = [
    "anyio>=4.5",
    "mcp>=1.28.1,<2",
    "toon-format==0.9.0b1",
    "pefile",
    "cryptography>=43",
    "pywin32; sys_platform == 'win32'",
    "Pillow>=10",
    "psutil>=6.0",
    "PyYAML>=6.0",
    "python-dotenv>=1.0,<2",
    "python-xlib>=0.33; sys_platform == 'linux'",
]

[project.urls]
Repository = "https://github.com/ROCTUP/1c-testpilot"

[project.scripts]
"1c-testpilot" = "_cli:main"

[project.optional-dependencies]
test = ["pytest>=8,<10"]
allure = ["pytest>=8,<10", "allure-pytest>=2.13,<3"]

[project.entry-points.pytest11]
testpilot = "testpilot_pytest"

[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = { "" = "app" }
py-modules = ["server", "_runtime", "_client_shutdown", "_mcp", "testpilot", "testpilot_pytest", "_cli", "tc1c", "guids", "_collection", "_response", "_refs", "_connections", "_profiles", "_code_execution", "_service_metadata", "_field_batches", "_row_search", "_snapshots", "_snapshot_tables", "_form_context", "_action_diagnostics", "_call_logging", "_log_reports", "_native_window", "_desktop_windows", "_desktop_linux", "_screenshots", "_screenshot_windows", "_screenshot_linux", "_screenshot_image", "tool_versions"]
